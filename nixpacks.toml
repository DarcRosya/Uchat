# Railway Nixpacks Configuration
[phases.setup]
nixPkgs = ["dotnet-sdk_9"]

[phases.build]
cmds = [
    "cd Uchat.Server",
    "dotnet restore",
    "dotnet publish -c Release -o out"
]

[start]
cmd = "cd Uchat.Server/out && dotnet Uchat.Server.dll"
